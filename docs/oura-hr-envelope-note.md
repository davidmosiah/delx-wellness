# Oura HR list envelope

`oura_list_heartrate` is a collection list, not agent-safe-series.
If agents need dense shape, prefer a future capped series tool or document list limits;
do not dump unbounded samples into context.
