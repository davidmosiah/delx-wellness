# STATUS series column

Registry/STATUS may note whether a connector exposes **agent-safe-series** tools
(`garmin_activity_series`, `strava_activity_series`, `fitbit_heart_series`, `polar_heart_series`).

Do not invent series tools for vendors without a real dense API.
