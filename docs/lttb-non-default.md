# LTTB optional method (non-default)

LTTB downsampling may be documented as optional `method`; default remains the existing server method.
Never silent-switch default downsample without a contract note.
