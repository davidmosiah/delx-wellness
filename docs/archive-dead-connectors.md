# Archive dead connectors

When a connector is no longer maintained:

1. Mark `unsupported` / archive reason in `registry.json` + STATUS
2. Record last-good npm version
3. Keep git history; do not pretend it is production-ready
4. Prefer honesty over catalog size (OSS operating mode)
