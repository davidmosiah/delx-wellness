# wellness-cgm glucose_series policy

Implement dense `glucose_series` **only** if a real agent user requests it with a use case.
Until then: summary tools + honest gap — no invented 1 Hz CGM stream.
