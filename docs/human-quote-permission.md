# Human quote permission

Never publish a user quote, screenshot handle, or testimonial without **explicit permission**.
Store a pointer (issue comment URL or private note path outside git) next to the quote.
