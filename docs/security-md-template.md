# SECURITY.md template (fleet)

- Tokens and health data stay **local** by default
- No cloud relay unless user runs their own
- Report leaked tokens in public issues → rotate + scrub
- Prefer GitHub private security advisories when available
